"""
Agents package — placeholder for Module 2.

Will contain one file per independent agent:
    code_analysis_agent.py
    security_vulnerability_agent.py
    remediation_agent.py
    pr_summary_agent.py
Each agent is its own class with its own prompt, invoked independently by
a pipeline orchestrator (also added in Module 2) — never combined into a
single LLM call.
"""

flask>=3.0
python-dotenv>=1.0
scikit-learn>=1.3
numpy>=1.26

# Real static-analysis libraries used by the Code Analysis / Security
# Vulnerability agents (in addition to Python's built-in `ast` module,
# which needs no install). Without these, the agents automatically fall
# back to a lightweight regex scanner — install these for the real thing.
radon>=6.0           # cyclomatic complexity + Maintainability Index (Python)
bandit>=1.7          # industry-standard Python SAST — the actual security scan
javalang>=0.13       # pure-Python Java parser, used for Java AST analysis

# Optional — only needed if you want the agents to call a real Gemini model
# instead of the built-in offline rule-based analyzer. The app runs fully
# without this package and without an API key.
google-genai>=0.3

# Milestone 4 — Code Review Report Generation Module (PDF export).
reportlab>=4.0
